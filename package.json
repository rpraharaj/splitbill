{
  "name": "settleup-app",
  "version": "1.0.0",
  "description": "SettleUp is an elegant and intuitive web application that simplifies group expense tracking and bill splitting. It features a polished UI for a seamless experience on both mobile and desktop devices.",
  "private": true,
  "scripts": {
    "dev": "esbuild index.tsx --bundle --outfile=dist/bundle.js --format=esm --jsx=automatic && serve -s . -l 3000"
  },
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "serve": "^14.2.3",
    "esbuild": "^0.20.2"
  }
}